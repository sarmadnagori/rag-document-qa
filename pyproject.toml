[project]
name = "semanticsearch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "ollama>=0.6.2",
    "postgres>=4.0",
    "uvicorn>=0.52.0",
]
